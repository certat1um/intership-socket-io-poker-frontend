//export const isRoomExists = async (roomCode) => {
//  const response = await fetch(
//    `http://localhost:8080/api/existing-room/${roomCode}`,
//  );

//  const result = await response.json();

//  return result;
//};
